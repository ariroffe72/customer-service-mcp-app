# ─── SMTP Configuration ───────────────────────────────────────────────
# Configure these to enable email delivery for support tickets.
# Without these, tickets are logged to the console instead.

SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=your-email@gmail.com
SMTP_PASS=your-app-password

# ─── Support Email ────────────────────────────────────────────────────
# The email address that receives support tickets.
SUPPORT_EMAIL=support@yourcompany.com

# ─── Server ───────────────────────────────────────────────────────────
PORT=3001
